<div class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-half">
        <h1 class="title">Editing reply</h1>
        <%= simple_form_for([@gossip, @comment]) do |f| %>
          <%= f.error_notification %>
          <div class="field">
            <div class="control">
              <%= f.input :message,
                          input_html: { class: "textarea" },
                          wrapper: false,
                          label_html: { class: "label" }
              %>
            </div>
          </div>

          <div class="field">
            <div class="control">
              <%= f.button :submit, "Update reply", class: "button is-info" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>