<section class="section">
  <div class="container">
    <div id="main" class="columns">

      <div class="column is-one-quarter">
        <% if user_signed_in? %>
          <%= render 'shared/profile_description' %>
        <% end %>
        <%= render 'shared/tags' %>
      </div>

      <%= render 'feed' %>
      <%= render 'shared/users' %>
    </div>
  </div>
</section>
